import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hand.magic.trick',
  appName: 'hand-magic-trick',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
